export type TravelPackage = {
  id: string;
  title: string;
  slug: string;
  category: string;
  shortDescription: string;
  fixedPrice: number;
  includedKm: number;
  durationHours: number;
  imageUrl: string;
};